# Core dependencies
rich>=13.0.0
requests>=2.31.0
websocket-client>=1.6.0
paramiko>=3.3.0

# RunPod SDK (if available, otherwise will use direct API calls)
runpod>=1.5.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
